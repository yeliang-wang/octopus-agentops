#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
TOOLKIT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

exec "${PYTHON:-python3}" "$TOOLKIT_ROOT/sandbox/octopus_sandbox.py" "$@"
